# İşletim sistemi kalıntıları
.DS_Store
Thumbs.db

# Composer bağımlılıkları (sunucuda `composer install` ile üretilir)
/vendor/

# Ortam değişkenleri (şifre içerir, ASLA git'e girmez)
.env

# Yüklenen dosyalar (kod değil, kullanıcı verisi)
/public/uploads/*
!/public/uploads/.gitkeep

# Geliştirici geçici dosyaları
hash.php
